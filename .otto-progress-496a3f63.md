# Task Progress: [WF] Build OMS API Key Manager & Secrets Vault / Step 3: Fix Review Issues

## Completed
- Fixed all 2 Critical + 2 Warning + 2 Suggestion issues from code review
- Committed: be1ace8
- Restarted otto-memory service — health OK
- Verified all 8 test cases pass against live API

## Fixes Applied

### Critical (both fixed)
1. `/secrets/get/{key_name}` unauthenticated access — now returns 401 if neither bearer token nor X-Otto-Service header present
2. `_check_auth()` empty-token bypass — now returns 503 (deny-by-default) when web_auth_token is not configured; ALLOW_UNAUTHENTICATED=true escape hatch for dev

### Warnings (both fixed)
3. `set_secret` action detection — replaced fragile audit-log query with PostgreSQL xmax=0 trick (RETURNING id, (xmax = 0) AS is_new)
4. VAULT_MASTER_KEY format documented in _get_fernet() docstring

### Suggestions (both fixed)
5. `import json` moved to top-level in vault.py
6. key_name slug validator added to SecretCreate (lowercase alphanumeric + underscore, max 128)

## Remaining
Nothing — task complete.
