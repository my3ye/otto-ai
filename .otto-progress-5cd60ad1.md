# Task Progress: Setup Deepgram voice note transcription for WhatsApp via Baileys

## Completed
- Read service.mjs — full understanding of current structure
- Identified audioMessage detection already present (lines 87-96), but only logs placeholder text

## In Progress
- Adding Deepgram API key to ~/memory/.env
- Adding transcribeAudio() function to service.mjs
- Wiring audio download + transcription into message handler

## Remaining
- Test/verify restart of whatsapp.service
- Commit changes

## Approach
- Use downloadContentFromMessage(audioMsg, 'audio') to get stream, collect as Buffer
- POST raw buffer to Deepgram pre-recorded API with nova-2 model (native Node 22 fetch)
- Replace text with "[Voice] <transcription>" before gateway dispatch
- Fallback: if transcription fails, keep original "[Audio message Xs]" text
- No new npm packages needed (Node 22 has native fetch)
