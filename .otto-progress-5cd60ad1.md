# Task Progress: Setup Deepgram voice note transcription for WhatsApp via Baileys

## Completed
- Added DEEPGRAM_API_KEY to ~/memory/.env
- Added `transcribeAudio()` function to service.mjs (lines 125-174):
  - Downloads audio stream via `downloadContentFromMessage(audioMsg, 'audio')`
  - Collects stream into Buffer, POSTs to Deepgram nova-2 pre-recorded API
  - URL: `https://api.deepgram.com/v1/listen?model=nova-2&smart_format=true&punctuate=true&language=en`
  - Returns transcript string or null on failure
- Wired into messages.upsert handler: audio/ptt messages get transcribed,
  finalText set to `[Voice] <transcript>`, stored in metadata.attachment
- Moved interfaces/ into ~/otto/interfaces/ with symlink ~/interfaces -> ~/otto/interfaces/
  (makes the code git-trackable while keeping systemd service path intact)
- Committed to otto git repo as `d6f93b7`
- **VERIFIED LIVE**: Service transcribed test voice note end-to-end:
  "Hey. I'm just testing, if, transcription works. So I'm just sending a voice note."
  Otto replied: "It works. Loud and clear."

## Approach
- Native Node 22 `fetch` (no new npm packages)
- Content-Type: `audio/ogg` (Baileys PTT format)
- Graceful fallback: if transcription fails, original `[Audio message Xs]` text is preserved
- DEEPGRAM_API_KEY constant reads from env, falls back to hardcoded key
