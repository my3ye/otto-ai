# Task Progress: Audit & update all landing pages via personas + spin up Projects DB Docker

## Completed
- Projects DB Docker: PostgreSQL 17 + FastAPI REST API LIVE on port 8201
  - Proxy via mev.otto.lk/papi (nginx)
  - 16 ecosystem projects pre-seeded with correct statuses
  - Waitlist endpoint: POST /papi/projects/{slug}/waitlist
- ONEON (oneon.ink): Fixed critical bug — email+handle collected but never submitted to API. Added API call after step 10 → commits waitlist signup to Projects DB. Pushed to ottomev/oneon-web main.
- MY3YE (my3ye.xyz): Added hero CTAs — "Explore the Protocols" + "Follow the Build". Pushed to ottomev/my3ye-web main.
- Panik App (panik.app): Already fully wired — Hero + Navbar have WaitlistModal → Projects DB API. No changes needed.
- Tusita (tusita.xyz): Already has WaitlistSection component → Projects DB API. No changes needed.
- 505 Systems (505.systems): Intentional no-waitlist design (community/governance). Correct as-is.
- WebAssist (webassist.ink): Live, footer says "powered by Ottolabs". Standalone narrative correct.

## Remaining
- Vercel deployments propagating for ONEON + MY3YE

## Approach
- Built Projects DB Docker as foundation first
- Fixed ONEON silent data loss bug
- Added MY3YE hero CTAs (first fold had zero action before)
- All 16 ecosystem projects seeded in DB
