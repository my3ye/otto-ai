# Task Progress: [P1] Remove sendLeadConfirmation — Resend is ecosystem-only per Mev
## Completed
- Removed `sendLeadConfirmation()` function and `LeadEmailData` interface from `lib/email.ts`
- Updated scope comment to: "Resend (otto.lk) is reserved for ecosystem & partnership outreach and invoicing ONLY. Not for lead confirmations or internal notifications."
- Kept Resend client init (import + `resend` + `FROM_EMAIL` exports) for future outreach/invoicing use
- Removed all 3 `sendLeadConfirmation()` call sites from `app/api/wizard/submit/route.ts`
- Removed the import line `import { sendLeadConfirmation } from '@/lib/email'` from route.ts
- TypeScript check: exit 0 (clean)
- Committed: b97026b
- Pushed to ottomev/web-assist main ✓

## In Progress
- Nothing

## Remaining
- Nothing — task complete

## Approach
- Pure removal: no new logic added, only deleted
- Resend client kept intact for future ecosystem/invoicing use
- WhatsApp `notifyMev()` path left untouched (that's how Mev gets notified)
