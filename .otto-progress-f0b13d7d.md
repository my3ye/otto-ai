# Task Progress: Set up email server as core Otto capability

## Completed
- Assessed environment: ports 587/465 OPEN, port 25 blocked by GCP (normal)
- Discovered otto.lk already has MX → smtp.google.com (Google Workspace started)
- No SPF/DKIM/DMARC set up yet for otto.lk
- No MTA installed on VM (correct — will use SMTP relay)
- Python stdlib email libs available (smtplib, imaplib)
- Interfaces/email/ directory exists but empty

## In Progress
- Building email_service.py (core send/receive module)
- Building email API routes for otto memory API

## Remaining
- Add email routes to api.py
- Test module loads correctly
- Commit everything
- Document DNS requirements for Mev

## Approach
- Use Gmail/Google Workspace SMTP (smtp.gmail.com:587 with TLS) for sending
- Use Gmail IMAP (imap.gmail.com:993) for receiving
- Backend also supports generic SMTP/IMAP for Resend/Mailgun as fallback
- Credentials flow: SMTP_USER + SMTP_PASSWORD (App Password from Google Workspace) in ~/memory/.env
- API endpoints: POST /email/send, GET /email/inbox, POST /email/check
- Otto address: otto@otto.lk (pending Google Workspace account from Mev)
