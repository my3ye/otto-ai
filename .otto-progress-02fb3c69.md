# Task Progress: [P1] Fix Resend scope — remove admin email, change domain to otto.lk

## Completed
- Removed `sendAdminLeadNotification()` entirely from `lib/email.ts`
- Changed `FROM_EMAIL` from `hello@webassist.ink` → `hello@otto.lk`
- Added scope comment at top of `email.ts`
- Removed all 3 `sendAdminLeadNotification()` calls from `app/api/wizard/submit/route.ts`
- Updated import in route.ts (removed `sendAdminLeadNotification` from import)
- Updated `.env.example` comment to reflect correct scope
- TypeScript check: clean (exit 0)
- Commit: 63b92a4 — pushed to ottomev/web-assist main ✅

## In Progress
- Nothing

## Remaining
- Nothing — task complete

## Approach
- Minimal scope reduction: removed ~72 lines (the admin email function + 3 call sites)
- No new functionality added
- WhatsApp admin notification (`notifyMev`) untouched — continues to work as before
