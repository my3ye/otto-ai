# Task Progress: [P2] Complete Lead Management wiring — verify CRM tables + fix gaps

## Completed
- Verified backend API routes (memory/routes/webassist.py commit 845eb33) — all CRM endpoints functional
- Tested /webassist/crm/leads and /webassist/crm/stats — both return gracefully (empty data, no errors)
- Confirmed webassist/stats returns `schema_ready: false` correctly (tables not yet created)
- TypeScript check passes (exit 0) — no frontend build errors
- Full Next.js build passes (18/18 pages including /webassist/leads)
- Added "Leads" direct nav item in OMS sidebar (commit e06727d) — `/webassist/leads` with Users icon
- Pushed to ottomev/web-next — Vercel deploy triggered
- Verified leads page has full functionality: Kanban + List views, search, lead detail sheet with stage/tier/contact info/notes/comms/stage-transition buttons

## In Progress
- N/A — all completable work done

## Remaining — BLOCKED (Needs Mev)
- **CRM Supabase tables need manual migration**: `crm_leads`, `lead_notes`, `lead_comms` do NOT exist yet
- Migration SQL files ready:
  1. `/mnt/media/projects/web-assist/supabase-full-schema.sql` (430 lines — base schema: wizard_completions, user_sessions, projects, etc.)
  2. `/mnt/media/projects/web-assist/supabase-crm-migration.sql` (crm_leads, lead_notes, lead_comms + trigger to auto-create CRM leads from wizard submissions)
- **Mev action**: Go to Supabase dashboard → SQL Editor → run full-schema.sql first, then crm-migration.sql
- URL: https://supabase.com/dashboard/project/dxptgrftyvsxabukvdlw/sql/new

## Approach
- Backend uses `_supabase_get_safe()` so it returns [] instead of raising 404 when tables don't exist — frontend shows empty state gracefully
- All frontend TypeScript is clean, build passes — ready for when migration is applied
- Migration has a trigger: new completed wizard submissions auto-create CRM leads
