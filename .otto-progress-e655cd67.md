# Task Progress: [P2] Fix Athena WhatsApp — create service + OMS connect flow

## Completed
- **athena-whatsapp.service**: Created `/etc/systemd/system/athena-whatsapp.service`, enabled, started (active since Mar 18 16:05 +0530). Service runs service.mjs on port 3002 with /health, /qr, /send HTTP endpoints.
- **Athena service.mjs**: Full-featured WhatsApp service at `/home/web3relic/interfaces/athena-whatsapp/service.mjs` — handles QR generation, message forwarding to gateway, /qr endpoint, dedup, liveness monitor.
- **Memory API /whatsapp/accounts**: Returns both Otto (connected) and Athena (needs_qr) with full status, committed in `f3c71da` (memory/routes/whatsapp.py +43 lines).
- **Memory API /whatsapp/accounts/athena/qr**: Proxies QR from port 3002, returns 237-char QR string, committed in `f3c71da`.
- **Memory API /whatsapp/accounts/athena/reconnect**: Triggers reconnect to get new QR, committed in `f3c71da`.
- **Memory API send-test**: Fixed to support Athena account (port 3002), committed in `b8156f3`.
- **OMS /whatsapp page**: Full page at `web-next/src/app/whatsapp/page.tsx` with:
  - QR code auto-display when Athena is in `needs_qr` state
  - **Auto-refresh QR every 15s** with countdown timer (QR codes expire ~20s) — committed in `9830aa2`
  - Refresh QR button, Connect button, Reconnect button for each state
  - Mobile responsive (flex-wrap, overflow-x-auto, md:grid-cols-2)
  - Status badges: connected/disconnected/needs_qr/needs_auth/no_service

## Status
COMPLETE — Mev needs to scan the QR at mev.otto.lk/whatsapp to link Athena's +94743768830.
The QR auto-refreshes every 15 seconds so it never goes stale.

## Key commits
- otto repo: `f3c71da` (Memory API routes), `b8156f3` (send-test fix)
- web-next repo: `52fe703` (initial page), `65208b2` (QR flow), `e75c478` (Connect button), `9830aa2` (auto-refresh QR)
- athena-whatsapp service: `/etc/systemd/system/athena-whatsapp.service` (systemd unit)
