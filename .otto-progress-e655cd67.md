# Task Progress: [P2] Fix Athena WhatsApp — create service + OMS connect flow

## Completed
- Audited existing code: athena-whatsapp service.mjs exists (port 3002), built QR endpoint
- Created `/etc/systemd/system/athena-whatsapp.service`, enabled, started (active since 16:05)
- Service generates QR codes continuously (auth_state was empty — old creds expired)
- Memory API: `/whatsapp/accounts` returns Athena as `needs_qr` with `has_qr: true`
- Memory API: `/whatsapp/accounts/athena/qr` proxies QR data from port 3002
- Memory API: `/whatsapp/accounts/athena/reconnect` endpoint working
- Memory API: `send-test` now supports Athena account (port 3002)
- OMS frontend: auto-fetches QR when `needs_qr`, displays via qrserver.com
- OMS frontend: "Refresh QR" button for `needs_qr` status
- OMS frontend: "Connect" button for `disconnected` Athena status
- Rebuilt and deployed static export, pushed to GitHub (commit e75c478)
- Mobile responsive at 320px, 375px, 768px (flex-wrap, overflow-x-auto, md:grid-cols-2)

## Status
COMPLETE — Athena service running, QR available, OMS shows QR for scanning.
Mev needs to: open mev.otto.lk/whatsapp, see the QR, scan with +94743768830 WhatsApp.

## In Progress
- Nothing — waiting for Mev to scan QR

## Remaining
- Mev scans QR to connect Athena

## Approach
- Service: systemd unit pointing to service.mjs on port 3002 with /qr + /health HTTP endpoints
- QR: service stores latest QR in memory, exposes via /qr endpoint
- API: /whatsapp/accounts/athena/qr proxies from port 3002
- Frontend: QR auto-displays when needs_qr, qrserver.com renders QR image
