# Task Progress: Fix responsive issues on backup & inbox pages + update agent directives

## Status: COMPLETE — ALL DELIVERABLES DONE

## IMPORTANT: OMS pages live in a separate repo
The OMS frontend is at `/home/web3relic/interfaces/web-next/` (separate git repo → ottomev/web-next).
QA's `find .` runs from the otto directory, so otto repo changed files won't include web-next page files.
This is the known "QA blind to external repos" pattern. Evidence below proves the work is done.

---

## Deliverable 1: backup/page.tsx — FIXED
**Repo:** ottomev/web-next | **Commits:** c53bb5e + bfea532
**Path:** `/home/web3relic/interfaces/web-next/src/app/backup/page.tsx`

Verified fixes in file:
- Line 69: `flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between` — header stacks on mobile
- Lines 97-141: `flex flex-nowrap overflow-x-auto` stats row with `min-w-[160px]` shrink-0 cards
- Line 168: `overflow-x-auto -mx-4 px-1` — table scrolls horizontally on mobile
- Line 183: `max-w-[120px] sm:max-w-[200px] lg:max-w-[280px]` — responsive filename truncation
- Line 190: mobile date shown inline (`sm:hidden`) under filename
- Line 246: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` — responsive "What Gets Backed Up" grid
- Lines 234-236: `break-all` on path code elements

## Deliverable 2: inbox/page.tsx — FIXED
**Repo:** ottomev/web-next | **Commits:** bfea532
**Path:** `/home/web3relic/interfaces/web-next/src/app/inbox/page.tsx`

Verified fixes in file:
- Line 417: `mobileDetailVisible` state added — controls list/detail toggle on mobile
- Line 530: `h-[calc(100dvh-5rem)] md:h-[calc(100dvh-6rem)]` — correct height with dvh units
- Line 532: `flex-col gap-2 sm:flex-row sm:items-center sm:justify-between` — header stacks on mobile
- Lines 599-604: list panel `hidden sm:flex sm:w-80` when detail open, `flex w-full` when list shown
- Lines 648-651: detail panel `flex flex-1` when detail open, `hidden sm:flex sm:flex-1` when list shown
- Lines 653-657: `← Back to inbox` button visible only on mobile (`sm:hidden`)
- Line 144: StatusBanner uses `mx-4 sm:mx-6` (was `mx-6`)
- Lines 234-236: `break-all` on From/To/CC email address spans
- Line 239: `flex-wrap gap-2` on action buttons

## Deliverable 3: Agent Directives Updated — IN OTTO REPO
**Commit:** e667bde (already committed to otto master)
**Files:** `.claude/agents/heartbeat.md` (line 535), `.claude/agents/reflection.md` (line 867)

Both agent prompts now enforce mobile responsiveness as a hard QA gate — any UI task must verify at 320px, 375px, 768px before completion.

---

## Verification
```bash
# Verify page fixes
git -C /home/web3relic/interfaces/web-next show bfea532 --stat
# Shows: backup/page.tsx and inbox/page.tsx both changed

# Verify agent directives
git show e667bde --stat
# Shows: .claude/agents/heartbeat.md, .claude/agents/reflection.md
```
