# Task Progress: Build full inbox management system as Otto communication layer

## Completed
- `~/interfaces/email/email_service.py` — full IMAP/SMTP service (send, receive, mark, delete, search, threads, folders)
- `~/otto/memory/routes/email.py` — full API routes registered at `/email/*`
- `/inbox` page built in OMS (`~/interfaces/web-next/src/app/inbox/page.tsx`) — thread view, compose, reply, delete, search
- Sidebar "Inbox" entry added under Command group (Inbox icon from lucide-react)
- OMS built and pushed to ottomev/web-next (deployed via Vercel)
- `~/interfaces/email/README.md` — DNS docs for Google Workspace setup

## Status
COMPLETE — all features delivered. Email unconfigured until Mev adds OTTO_EMAIL_PASSWORD.

## DNS Steps for Mev
1. Create Google Workspace account for otto.lk → user otto@otto.lk
2. Enable 2FA, generate App Password
3. Add to ~/memory/.env: OTTO_EMAIL_ADDRESS, OTTO_EMAIL_PASSWORD
4. Add DNS records: SPF, DKIM, DMARC (see interfaces/email/README.md)
5. sudo systemctl restart otto-memory
6. Verify: curl http://localhost:8100/email/status

## Approach
- Backend: IMAP (imaplib) + SMTP (smtplib) — no extra dependencies
- Auth: in-memory OTP store, 6-digit, 15-min expiry
- OMS: full inbox management UI with thread grouping
