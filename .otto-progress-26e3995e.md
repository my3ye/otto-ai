# Task Progress: Persist and display context injection history in OMS
## Completed
- DB migration 055: `context_injections` table with indexes on created_at and trigger
- Auto-persist in `/context/inject` endpoint: every call stores trigger, source, max_tokens, token_estimate, context_text
- API endpoints: `GET /context/injections` (list, filterable by trigger), `GET /context/injections/{id}` (detail), `GET /context/injections/{id}/diff` (unified diff vs previous of same trigger type)
- OMS page at `/context-history/page.tsx`: chronological list, trigger filter, full text view, diff view with +/- stats
- Sidebar nav entry: "Context History" with History icon
- API types in `api-types.ts`: ContextInjectionSummary, ContextInjectionDetail, ContextInjectionDiff, ContextInjectionList
- Committed: `255717e feat(oms): add context injection history page with diff view`
- Verified: 3 injections recorded, diff API returning proper unified diffs with token deltas
## In Progress
- Nothing
## Remaining
- Nothing — fully complete
## Approach
- Auto-persist on every /context/inject call (no hook changes needed — all sources already call this endpoint)
- Trigger mapped from source param
- Diff as server-side unified diff via difflib (Python)
- OMS: split panel (list left, detail/diff right)
