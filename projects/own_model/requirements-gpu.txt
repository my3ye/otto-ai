# Otto GPU Training Dependencies
# Install on RunPod or any GPU instance with: pip install -r requirements-gpu.txt
#
# Tested on: RTX 4090 (24GB), CUDA 12.4, Python 3.11
# Approx install time: 5-10 minutes on RunPod

# Core ML
torch>=2.4.0
torchvision>=0.19.0

# HuggingFace stack
transformers>=4.48.0
tokenizers>=0.21.0
datasets>=3.3.0
accelerate>=1.3.0
huggingface-hub>=0.28.0

# LoRA / PEFT
peft>=0.14.0

# 4-bit quantization (BitsAndBytes NF4)
bitsandbytes>=0.45.0

# SFT training
trl>=0.14.0

# Tokenization for Qwen
sentencepiece>=0.2.0
protobuf>=4.25.0

# Data handling
numpy>=1.24.0

# Utilities (for runpod_launch.py orchestrator)
requests>=2.32.0

# Optional: Unsloth for 2-5x faster training (alternative to PEFT)
# Uncomment if using train.py instead of train_gpu.py
# unsloth[cu124-torch240] @ https://github.com/unslothai/unsloth/releases/download/...
