# Task Progress: [WF] Complete all BANKR features and update OMS management system / Step 3: Fix Review Issues

## Completed
- (nothing yet)

## In Progress
- Reading crypto/page.tsx to understand current state

## Remaining
- Fix React fragment missing `key` (lines ~850, ~941)
- Fix silent error swallowing in `handleCancel` (line ~564) and `handleClose` (line ~806)
- Fix duplicate `/crypto/status` fetch (lines ~1165-1168 vs ~208-211)
- Optional: portfolio chains hardcoded fix
- Optional: add `launch` feature to status grid

## Approach
- Read the full file first, then make targeted fixes one at a time
