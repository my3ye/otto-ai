# Task Progress: [WF] Complete all BANKR features and update OMS management system / Step 3: Fix Review Issues

## Completed
- Fixed React Fragment key: `<>` → `<React.Fragment key={s.id}>` in signals table
- Fixed silent error in `handleCancel`: added `cancelError` state + banner display
- Fixed silent error in `handleClose`: added `closeError` state + inline display
- Fixed duplicate `/crypto/status` fetch: StatusSection now accepts data/loading props from CryptoPage
- Added `launch` feature flag to ApiFlag grid
- Build: ✓ Compiled successfully in 19.2s
- Committed: 2ea62bc, pushed to ottomev/web-next

## In Progress
- DONE

## Remaining
- Nothing

## Approach
- All 3 warnings + 1 suggestion addressed. Skipped portfolio chains hardcode (low priority, read-only display).
